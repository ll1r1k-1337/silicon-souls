{
  "name": "silicon-souls",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first chat workspace where you talk to AI agents via @handle.",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install",
    "build:web": "npm --prefix web run build",
    "build:server": "npm --prefix server run build",
    "build": "npm run build:web && npm run build:server",
    "start": "node server/dist/main.js",
    "start:after-build": "npm run build && npm start",
    "dev:server": "npm --prefix server run start:dev",
    "dev:web": "npm --prefix web run dev",
    "clean": "rm -rf server/dist web/dist"
  }
}
